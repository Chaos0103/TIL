#### 깃 버전 확인
`git --version`

#### 아이디 등록(최초 1회)
`git config --global user.name <아이디>`

#### 이메일 등록(최초 1회)
`git config --global user.email <이메일주소>`

#### 경로 이동
`cd /경로/경로/경로`

#### 클론
`git clone <레퍼지토리 주소>`

#### 파일 추가
`git add <파일명>`

#### 커밋
`git commit -m "문구"`

#### 푸쉬
`git push`
